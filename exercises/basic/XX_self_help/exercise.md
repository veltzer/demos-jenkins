# Jenkins and self-help

* We want to generate a self-help dummy app which receives two parameters: `a` and `b`
    and shows their sum.

* Create a pipeline with parameters, but *without* a git tree.

* Show that your pipeline works.
